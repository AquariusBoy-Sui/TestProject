import SwiftUI

extension Color {
    
    static var background: Color { Color("background") }
    static var unbackground: Color { Color("unbackground") }
    static var textColor: Color { Color("text_color") }
    static var untextColor: Color { Color("untext_color") }
}
